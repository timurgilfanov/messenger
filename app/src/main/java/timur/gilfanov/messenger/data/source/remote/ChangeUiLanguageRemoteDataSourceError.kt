package timur.gilfanov.messenger.data.source.remote

/**
 * Errors that can occur during remote language change operations.
 *
 * Type alias to [RemoteSettingsDataSourceError] as language change operations
 * use the same error types as other remote settings operations.
 */
typealias ChangeUiLanguageRemoteDataSourceError = RemoteSettingsDataSourceError
