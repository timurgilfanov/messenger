package timur.gilfanov.messenger.data.source.remote

/**
 * Errors that can occur during remote language change operations.
 *
 * Type alias to [RemoteUserDataSourceError] as language change operations
 * use the same error types as other remote user data operations.
 */
typealias ChangeUiLanguageRemoteDataSourceError = RemoteUserDataSourceError
